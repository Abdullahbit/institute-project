[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm --filter @institute/api run build"

[deploy]
startCommand = "pnpm --filter @institute/api run start"
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
